CREATE index contract_end_index on Supplier(contract_end);

CREATE index quantity_index on Part(quantity);

CREATE index phone_number_index on Customer(phone_number);

CREATE index brand_name_index on Brand(brand_name);